# This is a factorial program using Pthreads and GMP, written in C

* Usage: compile with `make clean all`
* To modify the runtime: change the `THREAD_NUM` and `FACTORIAL` macros found in `factorial.c`